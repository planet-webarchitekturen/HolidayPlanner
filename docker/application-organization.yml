spring:
  kafka:
    bootstrap-servers: kafka:29092
  datasource:
    url: jdbc:postgresql://postgres:5432/organization_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
